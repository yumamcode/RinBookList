document.addEventListener("DOMContentLoaded",()=>{
	const tableOptions = {
		"info" : true,
		"paging": true,
		"searching": false,
		"ordering": false,
	}
	const $bookTable = $("#bookTable").DataTable(tableOptions);
	}
	);